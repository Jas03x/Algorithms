
bool AVL_Trees();
bool Max_Heaps();
bool Bubble_Sort();

int main(int argc, char* argv[])
{
	bool status = true;

	if (status)
	{
		// status = AVL_Trees();
	}

	if (status)
	{
		// status = Max_Heaps();
	}

	if (status)
	{
		status = Bubble_Sort();
	}

	return status ? 0 : -1;
}
